import React from "react";
import ReactDOM from "react-dom";
import App from "./Components/App.js";

ReactDOM.render(<App />, document.querySelector("#root")); //this runs once; when we first run the program (returns null) then again with our current latitude
